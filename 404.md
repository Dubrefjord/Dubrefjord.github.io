---
title: "404 — Page not found"
permalink: /404.html
layout: single
author_profile: false
sitemap: false
excerpt: "The page you were looking for doesn't exist."
---

<style>
  /* Scoped styles for the 404 page only.
     Brand palette is also defined globally in main.scss; values
     are duplicated here so the page renders correctly even if it
     loads before the main stylesheet. */
  .nf {
    --nf-navy-deep:  #1A1C34;
    --nf-navy:       #262D51;
    --nf-indigo:     #444D7C;
    --nf-cream:      #F7F5F2;
    --nf-cream-soft: #EFF4EE;
    --nf-red:        #E63946;

    text-align: center;
    padding: 4rem 1.5rem 5rem;
    max-width: 640px;
    margin: 0 auto;
  }

  .nf__code {
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(5rem, 18vw, 9rem);
    line-height: 1;
    color: var(--nf-navy);
    margin: 0 0 0.25em;
    letter-spacing: -0.02em;
  }

  .nf__rule {
    width: 64px;
    height: 3px;
    background: var(--nf-red);
    border: 0;
    margin: 1.25rem auto 1.75rem;
  }

  .nf__lead {
    font-size: 1.15rem;
    color: var(--nf-navy-deep);
    margin: 0 0 0.5em;
    font-weight: 600;
  }

  .nf__sub {
    color: var(--nf-indigo);
    margin: 0 0 2.25rem;
    line-height: 1.55;
  }

  .nf__actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .nf__btn {
    display: inline-block;
    padding: 0.7em 1.4em;
    border-radius: 4px;
    font-weight: 600;
    text-decoration: none;
    transition: background 0.15s, color 0.15s, border-color 0.15s;
  }

  .nf__btn--primary {
    background: var(--nf-navy);
    color: var(--nf-cream);
  }
  .nf__btn--primary:hover {
    background: var(--nf-red);
    color: #fff;
  }

  .nf__btn--ghost {
    background: transparent;
    color: var(--nf-navy);
    border: 1.5px solid var(--nf-navy);
  }
  .nf__btn--ghost:hover {
    background: var(--nf-navy);
    color: var(--nf-cream);
  }
</style>

<div class="nf">
  <p class="nf__code">404</p>
  <hr class="nf__rule">
  <p class="nf__lead">This page wandered off.</p>
  <p class="nf__sub">
    The URL you followed doesn't lead anywhere on this site.
    It may have been moved, renamed, or never existed in the first place.
  </p>
  <div class="nf__actions">
    <a class="nf__btn nf__btn--primary" href="{{ '/' | relative_url }}">Back to home</a>
    <a class="nf__btn nf__btn--ghost" href="{{ '/posts/' | relative_url }}">Browse posts</a>
  </div>
</div>
